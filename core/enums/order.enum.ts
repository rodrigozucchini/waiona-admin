export enum OrderStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  DISPATCHED = 'dispatched',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
}

export enum DeliveryType {
  DELIVERY = 'delivery',
  PICKUP = 'pickup',
}
